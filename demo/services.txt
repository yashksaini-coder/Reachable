# Watched services — one `owner/repo [criticality 1-3]` per line. Replayed by `make demo`
# (each line becomes `make add REPO=…`, i.e. an ingest job); the console reads the `# core` / `# victim` section headers for the cohort chip; the pipeline reads the slugs.
#
# Two disclosed cohorts:
#   core    8 well-maintained repos verified 2026-08-16 to ship package-lock.json v3 with real
#           commit history; ~170 transitive deps are shared by all eight.
#   victim  4 repos found via GitHub code search for the malicious tarball names
#           (chalk-5.6.1.tgz, debug-4.4.2.tgz) whose package-lock.json history actually pinned a
#           compromised version — the real blast radius of the 2025-09-08 incident. Small
#           projects, because that is who runs `npm install` inside a 95-minute window.

# core
twbs/bootstrap 3
medplum/medplum 3
Kong/insomnia 3
documenso/documenso 3
louislam/uptime-kuma 2
usebruno/bruno 2
wagtail/wagtail 2
socketio/socket.io 1

# victim
GoMake-ltd/n8n-node-gomake 2
LVQT-ss/cakestory-api 1
lperry65/Aider-Chat 1
ChrisTregaskis/ai-research-automation 1

# added through the console (Services → add repository) while testing add-by-URL; kept as a third cohort
koajs/koa 1
