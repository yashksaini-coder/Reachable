import { NotFoundView } from "@/components/console/states";

// notFound() from a console page (unknown advisory / service) — already inside the shell.
export default function NotFound() {
  return <NotFoundView />;
}
